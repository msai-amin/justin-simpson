# Justin Simpson — personal site

A personal homepage for Justin Simpson, philosopher of **environment & technology**.

The whole page lives in two moods that one control swaps between:

- **Physis** (φύσις, _the self-arising_) — warm parchment, serif type, flowing organic contours.
- **Technê** (τέχνη, _the bringing-forth_) — graphite, monospace, a fine wireframe grid.

The form performs the philosophy: the toggle in the top-right re-skins the entire page so you
can read it under either aspect. The palette is drawn from Justin's portrait, whose painted
forms already flow from green to graphite.

Built with [Astro](https://astro.build) (static), self-hosted fonts, and no runtime
dependencies. Publications live as Markdown files, so the list is easy to extend.

---

## Run it

```sh
npm install
npm run dev        # http://localhost:4321
```

```sh
npm run build      # outputs static site to ./dist/
npm run preview    # preview the production build
```

## Deploy it (free)

`npm run build` produces a plain static `dist/` folder. Any of these work:

- **Netlify / Vercel** — import the repo; build command `npm run build`, publish dir `dist`.
- **GitHub Pages** — push `dist/` (or use the official Astro Pages action). If serving from a
  subpath like `user.github.io/justin`, set `site` and `base` in `astro.config.mjs`.
- **Cloudflare Pages** — same build settings.

A custom domain (e.g. `justinsimpson.xyz`) can be pointed at any of the above in their
dashboard.

## Add a publication

Copy any file in `src/content/publications/` and edit the frontmatter:

```yaml
---
title: "Full title of the work"
year: 2026
venue: "Journal or Press, vol(issue), pages"
type: "article"        # article | chapter | review
coauthors: ["Name"]    # optional
url: "https://doi.org/..."  # optional
order: 1                # optional — higher sorts first within a year
summary: "One sentence in plain language."
---
```

## Change the portrait

Replace `src/assets/justin.jpg` (the build optimizes it automatically). The current image was
converted from the original with `sips`.

---

## ✅ Before you share it

Most content now comes straight from Justin's CV. A few optional bits remain:

1. **Approve the bio.** Built from his CV + published work — edit in `src/components/About.astro`.
2. **Optional contact links.** Email is wired (`justin.simpson@utrgv.edu`). The Google Scholar
   and ORCID cards in `src/components/Contact.astro` are still `todo` — add URLs if he has them.
   (His personal cell from the CV was deliberately left off the public page.)
3. **Downloadable CV — done, but review it.** `public/cv.pdf` is a clean, public-safe copy
   generated by `scripts/build_cv_pdf.py` that **omits the References section and Justin's
   personal cell**. Regenerate anytime with `python3 scripts/build_cv_pdf.py`. Give it a once-over
   before sharing.
4. **One article pending.** "Moving Forward by Going Back" (Public Philosophy Journal) was left
   off because the CV gives no year and it couldn't be verified — add it once the year is known.

---

_Publications verified against journal records, June 2026 · book, title, email, and degrees from
Justin's CV · Heraclitus epigraph (fr. B123) in the footer._
